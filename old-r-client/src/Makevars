TARGET_DIR = ./rust/target
LIBDIR = $(TARGET_DIR)/release
STATLIB = $(LIBDIR)/liblicorclient.a
PKG_LIBS = -L$(LIBDIR) -llicorclient

all: $(SHLIB)

$(SHLIB): $(STATLIB)

$(STATLIB):
	cd rust && cargo build --release --lib